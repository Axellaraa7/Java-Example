//Pattern that allows to notify to the observer class what is happening with the obsevable class. It is composed of two elements, the object that is doing actions and the object that is watching every action the other object does. It uses an interface for the observer and an interface for the observable. The observable object can notify to multiple observer objects.
package patterns;

public class Observer{

}