//pattern that allows to have an unique instance of the class. It is very useful to avoid using extra resources of the server. To make it possible, this pattern use static variables and access modifiers in order to avoid the instantiation of the class in multiple ways.

package patterns;

public class Singleton(){
  
}